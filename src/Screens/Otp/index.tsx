import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

interface IProps {}

const OtpScreenScreen = (props: IProps) => {
  return (
    <View>
      <Text>index</Text>
    </View>
  );
};

export default OtpScreenScreen;

const styles = StyleSheet.create({});
